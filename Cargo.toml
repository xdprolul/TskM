[package]
name = "TskM"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = { version = "0.33", features = ["glow"] }
# egui_dnd = "0.14.0"

dirs-next = "2.0"

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
